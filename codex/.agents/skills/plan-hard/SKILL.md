---
name: plan-hard
description: DEPRECATED — use /plan --deep instead. This skill has been merged into the plan skill.
user-invocable: false
metadata:
  platforms:
    - all
---

# plan-hard — DEPRECATED

This skill has been removed. Use `/plan --deep` instead.

The deep research capability is now built into the `plan` skill natively.
